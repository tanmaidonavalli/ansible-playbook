---
 - hosts: all
   gather_facts: yes
   sudo: yes
 
   tasks:
    - copy:
        src: /etc/ansible/recurcopy
        dest: - task: CopyFiles@2
  inputs:
    SourceFolder: '/etc/ansible/recurcopy'
    Contents: '**/*yml'
    TargetFolder: '/etc'
